# Changelog

## 开发中

### 新增

- 增加 WorkBuddy 本地任务监测，识别处理、分析、工具执行、等待输入、等待确认和完成状态。
- Codex 与 WorkBuddy 任务增加各自的应用 Logo、状态点和来源底色。
- 设置页增加监测状态入口，可复制当前数据来源、可读性与活动任务数量等本地诊断信息。

### 修复与优化

- 兼容 Codex 桌面端转发的 `turn/started` 与 `turn/completed` 生命周期事件，减少部分任务状态缺失。
- 多来源任务继续共用原有固定画布、尺寸切换和状态动画，不为应用 Logo 增加新的面板高度。
- WorkBuddy 图标按应用标识查找，不依赖固定安装路径。

## v0.1.6 — 2026-07-27

### 新增

- 增加 Codex Voice 会话识别，语音期间在任务行显示波形与语音状态。
- 设置页增加 Voice 快捷键说明入口，继续由 Codex 管理实际全局快捷键。
- 额度用尽时显示来自本机 Codex 日志的预计重置时间。

### 修复与优化

- 使用桌面日志中的 `thread/realtime/start` 和 `thread/realtime/stop` 作为语音状态的最终依据。
- 修复 Codex 复用历史语音任务时，新语音状态被旧完成阶段覆盖的问题。
- 修复语音关闭后会话文件没有结束记录，导致刘海长期残留语音状态的问题。
- 剩余量低于或等于 1% 时按 0% 展示，避免清零后仍显示 1%。
- 移除日报彩带动画，降低额外动画负担并保持统一状态过渡。

## v0.1.5 — 2026-07-26

### 新增

- 增加真实多 Agent 状态：任务行显示子 Agent 数量，点击后展开父任务与子任务层级。
- 增加当前工具活动图标与动作名称，包括绘图、网页浏览、界面检查、代码修改和 GitHub 同步等状态。
- 增加 Queue 计数，仅统计本轮任务开始后追加且尚未处理的消息。

### 修复与优化

- 扩大大型图片与界面截图后的日志读取窗口，避免子 Agent 创建事件被大体积记录覆盖。
- 排除每轮初始用户消息，避免将正常任务请求误计为 Queue。
- Agent、工具和 Queue 信息仅位于刘海两侧安全区及下方任务行，中央物理摄像头区域保持空白。
- 新状态继续复用固定顶部画布、统一尺寸过渡和流光边框动画。

## v0.1.4 — 2026-07-23

### 新增

- 空闲悬停面板增加“今日节奏”，显示连续使用天数、今日强度评价与 14 天活跃进度。
- 点击“今日节奏”区域可直接打开本地 Token 日报。

### 修复与优化

- “今日节奏”复用既有空闲面板尺寸，不增加刘海展开高度。
- 新内容继续使用统一顶部画布与状态过渡，不改变任务、确认和设置面板的动画管线。

## v0.1.3 — 2026-07-23

### 新增

- 增加本地最近任务搜索，可按任务名称、项目与摘要筛选并返回对应 Codex 对话。
- 增加轻量自动更新检查，每 24 小时最多请求一次 GitHub Release。
- 设置页增加手动检查和新版本下载入口。
- 设置改为“通用 / 功能 / 关于”横向分类，并增加官方网站与 GitHub 入口。
- 增加一键发版脚本及维护者发布说明。
- 增加本地 Token 日报、14 天圆环热力、近 7 日个人基线评价与能量轨迹图。
- 日报增加连续使用天数、14 天活跃进度，以及高强度日期的一次性轻量庆祝动画。
- 完成任务时增加一次性“圆环绘制 → 对勾回弹”动画，并适配减少动态效果。
- 每天 18:30 后在首次空闲时提供 8 秒轻提醒，并支持从用量面板随时查看。
- 拖入网页、GitHub Skill、图片或普通文件后，根据内容提供不同的下一步操作。
- 额度首次低于 50% 时增加一次性橙色提醒，随后只保留颜色状态。

### 修复与优化

- 重启后优先恢复最近一次有效额度，避免等待新 Token 事件期间显示为 0。
- 所有新增面板继续使用固定顶部画布和统一过渡，避免设置、日报与拖入状态切换时出现裂隙。
- 移除日报提醒的内部预览启动入口，避免正式构建残留调试状态。

## v0.1.2 — 2026-07-21

### 新增

- 增加计划模式“需要用户输入”状态，使用橙色提醒并支持点击返回对应 Codex 对话。
- 根据 `request_user_input` 的 `call_id` 跟踪多轮提问，全部回答后自动恢复分析状态。
- 增加可记忆的任务整体收起按钮，收起后继续显示余量、任务阶段和执行数量。

### 修复与优化

- 区分“需要用户输入”和权限确认，避免误显示绿色确认操作。
- 收起与展开继续复用统一尺寸及边框动画管线。
- 根据当前 MacBook 的真实刘海安全区域调整收起态，中间保留 220pt，避免运行状态被摄像头遮挡。
- 保留旧版顶层输入事件兼容，适应不同 Codex 日志结构。

## v0.1.1 — 2026-07-21

### 新增

- 增加中文、英文和跟随系统语言切换。
- 增加 BoringNotch、NotchNook 冲突检测与隐藏确认。
- 增加绘图任务状态监测，避免大型图片日志导致任务状态丢失。
- 增加任务健康提示：长时间没有新事件时显示橙色状态。
- 增加今日 Token 的主要模型用量拆分。
- 空闲悬停区域支持点击空白位置快速打开 Codex。

### 修复与优化

- 修复设置展开引起的顶部裂隙和外层面板重排。
- 修复额度重置后剩余百分比更新延迟。
- 修复重置倒计时被版本信息挤压的问题。
- 修复模型上下文未被正确识别的问题。
- 减少会话文件的重复枚举，降低持续监测开销。

## v0.1.0 — 2026-07-20

- 首个公开预览版。
